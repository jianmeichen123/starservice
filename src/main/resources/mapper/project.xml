<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper  
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="com.galaxy.im.bean.project.ProjectBo">

  	<sql id="Base_Where_Clause">
		<where>
			<trim prefixOverrides="and">
			    <if test="id != null"> and id =#{id}</if>
				<if test="createUid != null" > and create_uid = #{createUid} </if>
				<if test="projectDepartid != null" > and project_departId = #{projectDepartid} </if>
				<if test="projectStatus != null" > and project_status = #{projectStatus} </if>
				<if test="projectType != null" > and project_type = #{projectType} </if>	
				<if test="projectProgress != null" > and project_progress = #{projectProgress} </if>
				<if test="financeStatus != null" >
			      	AND EXISTS(
					SELECT 1
					FROM information_title_relate t
					INNER JOIN information_result r ON t.title_id = r.title_id 
					WHERE r.project_id = sop_project.id AND t.code = 'FNO1_1' and t.report_type=4
					AND r.content_choose = #{financeStatus} 
					)
			    </if>
				<if test="faFlag != null"> and fa_flag = #{faFlag} </if>
				<!-- 模糊查询 -->
				<if test="keyword != null">
					<choose>
						<when test="flagkeyword=='concatcode'" >
							and (upper(project_name) like CONCAT("%",upper(#{keyword}),"%")
							<if test="escapeChar ">
								escape '/'
							</if>
							or create_uname like CONCAT("%",#{keyword},"%")
							<if test="escapeChar ">
								escape '/'
							</if>
							)
						</when>
						<when test="flagkeyword==null">
							and (upper(project_name) like CONCAT("%",upper(#{keyword}),"%")
							<if test="escapeChar ">
								escape '/'
							</if>
							or upper(project_code) like CONCAT("%",upper(#{keyword}),"%")
							<if test="escapeChar ">
								escape '/'
							</if>
							)
						</when>
						<when test="flagkeyword=='onlyName'">
							and upper(project_name) like CONCAT("%",upper(#{keyword}),"%")
							<if test="escapeChar ">
								escape '/'
							</if>
						</when>
					</choose>
				</if>
			</trim>
		</where>
		<if test="sorting != null">order by ${sorting}</if>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</sql>
	
  	<sql id="Base_Column_List" >
	    id, 
	    project_name projectName,
	    project_code projectCode, 
	    project_type projectType, 
	    stock_transfer stockTransfer, 
	    project_careerline projectCareerline, 
	    project_departId projectDepartId,
	    industry_own industryOwn, 
	    project_valuations projectValuations, 
	    final_valuations finalValuations, 
	    currency_unit currencyUnit,
	    service_charge serviceCharge,
	    project_company projectCompany, 
	    formation_date formationDate, 
	    project_company_code projectCompanyCode, 
	    company_legal companyLegal, 
	    create_uid createUid, 
	    create_uname createUname, 
	    create_uposition createUposition, 
	    project_progress projectProgress, 
	    project_status projectStatus, 
	    updated_time updatedTime, 
	    created_time createdTime,
	    project_describe projectDescribe, 
	    project_describe_financing projectDescribeFinancing, 
	    project_business_model projectBusinessModel, 
	    company_location companyLocation, 
	    user_portrait userPortrait,
	    prospect_analysis prospectAnalysis, 
	    next_financing_source nextFinancingSource,
	    industry_analysis industryAnalysis,
	    operational_data operationalData, 
	    idea_id ideaId, 
	    fa_flag faFlag, 
	    fa_name faName,
	    project_time projectTime,
	    (SELECT r.content_choose
				FROM information_title_relate t
				INNER JOIN information_result r ON t.title_id = r.title_id 
				WHERE r.project_id = sop_project.id AND t.code = 'FNO1_1' and t.report_type=4 ) AS financeStatus,
		(SELECT case when r.content_describe1 is null or r.content_describe1 ='' then null else r.content_describe1 end content_describe1
				FROM information_title_relate t
				INNER JOIN information_result r ON t.title_id = r.title_id 
				WHERE r.project_id = sop_project.id AND t.code = 'FNO1_2_1' and t.report_type=4 ) AS projectContribution ,
		(SELECT case when r.content_describe1 is null or r.content_describe1 ='' then null else r.content_describe1 end content_describe1
				FROM information_title_relate t
				INNER JOIN information_result r ON t.title_id = r.title_id 
				WHERE r.project_id = sop_project.id AND t.code = 'FNO1_2_3' and t.report_type=4 ) AS projectShareRatio ,
		(SELECT case when r.content_describe1 is null or r.content_describe1 ='' then null else r.content_describe1 end content_describe1
				FROM information_title_relate t
				INNER JOIN information_result r ON t.title_id = r.title_id 
				WHERE r.project_id = sop_project.id AND t.code = 'FNO1_3_1' and t.report_type=4 ) AS finalContribution ,
		(SELECT case when r.content_describe1 is null or r.content_describe1 ='' then null else r.content_describe1 end content_describe1
				FROM information_title_relate t
				INNER JOIN information_result r ON t.title_id = r.title_id 
				WHERE r.project_id = sop_project.id AND t.code = 'FNO1_3_3' and t.report_type=4 ) AS finalShareRatio 
   </sql>
   
   <!-- 查询总数 -->
	<select id="selectPageListCount" resultType="java.lang.Long" parameterType="java.util.Map">
		SELECT count(id) 
		FROM sop_project 
		<include refid="Base_Where_Clause" />
	</select>

	<!-- 查询 -->
	<select id="selectPageList" resultType="hashMap" parameterType="java.util.Map">
		SELECT 
		<include refid="Base_Column_List" />
		FROM sop_project 
		<include refid="Base_Where_Clause" />
	</select>
	
</mapper>